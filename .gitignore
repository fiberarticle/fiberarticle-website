# Dependencies
node_modules/

# Build output (dist-ssr is the throwaway Node copy the prerender step uses)
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
.DS_Store

# Heavy PNG source screenshots. Only the compressed WebP versions are
# shipped to the site, so the large originals are kept out of the repo.
public/product-screenshots/*.png
